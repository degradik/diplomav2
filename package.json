{
  "name": "diplomav2",
  "version": "1.0.0",
  "private": true,
  "description": "Веб-приложение для регистрации участников и управления мероприятиями",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run prisma:migrate --prefix backend && npm run seed --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/degradik/diplomav2.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/degradik/diplomav2/issues"
  },
  "homepage": "https://github.com/degradik/diplomav2#readme",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
